{
  "name": "vquail-dashboard",
  "version": "1.0.0",
  "description": "VQuail Poultry IoT Dashboard",
  "main": "index.html",
  "scripts": {
    "build": "node scripts/build-config.js",
    "dev": "node scripts/dev-server.js",
    "deploy:github": "npm run build && gh-pages -d dist",
    "deploy:vercel": "vercel --prod",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["iot", "poultry", "mqtt", "dashboard"],
  "author": "VQuail Team",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.0",
    "gh-pages": "^5.0.0",
    "vercel": "^28.0.0"
  }
}
